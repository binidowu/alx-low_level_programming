#include <stdio.h>

/**
 * main - Writes out the value of char variable to stdout using puts()
 * @char str: Stores the value of str to be printed out
 *
 * Return: Prints outs str value
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";
	puts(str);

	return (0);
}
